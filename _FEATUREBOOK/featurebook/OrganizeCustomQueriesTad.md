# Featurebook > OrganizeCustomQueriesTad.md
Go to [Featurebook > Index](FEATUREBOOK.md)

## TOC

* [`@Scenario` `_quickInstructions()`](#_quickInstructions)

## Scenarios

<a id="_quickInstructions"></a>
<table>
<tr><td> 

`@Scenario` `_quickInstructions()`<br />
</td></tr>
<tr><td>

Custom queries (in the right-hand bar) are grouped in `My custom queries` and `Custom queries`.

This feature:

![link.png](../featurebook-img/OrganizeCustomQueriesTad/_quickInstructions/link.png)

splits `Custom queries` into `Custom queries (this project)` and `Custom queries (all projects)`, using the same styling for the titles/"separators":

![after-org.png](../featurebook-img/OrganizeCustomQueriesTad/_quickInstructions/after-org.png)

Each CQ (all projects) has a link to open that CQ in "all projects mode".

![all-proj.png](../featurebook-img/OrganizeCustomQueriesTad/_quickInstructions/all-proj.png)

After the last CQ, there is a `<hr />` and then the helper text.

![helper.png](../featurebook-img/OrganizeCustomQueriesTad/_quickInstructions/helper.png)
</td></tr>
</table>
